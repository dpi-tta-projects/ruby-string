#!/usr/bin/env ruby

require 'bundler/setup'
require 'grade_runner'
require 'grade_runner/tasks'

GradeRunner.config do |config|
  config.override_local_specs = false
  config.submission_url = "https://grades.dpi.dev"
end
